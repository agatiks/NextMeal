import 'package:flutter/cupertino.dart';

class GeneratePage extends StatelessWidget{
  GeneratePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Generate");
  }

}