import 'package:flutter/cupertino.dart';

class DishesPage extends StatelessWidget{
  const DishesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Dishes");
  }

}